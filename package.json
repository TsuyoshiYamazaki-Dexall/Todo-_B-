{
  "name": "todo-app",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outfile=public/bundle.js --minify",
    "dev": "esbuild src/index.tsx --bundle --outfile=public/bundle.js --watch",
    "serve": "npx serve public -l 3000"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
